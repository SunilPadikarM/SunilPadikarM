@if (Session["UserId"] != null)
{
    using (Html.BeginForm("LogOff", "CustomerAccount", FormMethod.Post, new { id = "logoutForm", @class = "navbar-right" }))
    {
        @Html.AntiForgeryToken()

<ul class="nav navbar-nav navbar-right">
    <li><a href="../Reservations/TransactionSummary">Transaction Summary</a></li>
    <li>@Html.ActionLink("Feedback", "Index", "Feedback/Create")</li>
    <li>
        @Html.ActionLink("Hello " + Session["User Name"] + "!", "EditCustomer", "CustomerAccount", routeValues: null, htmlAttributes: new { title = "Manage" })
    </li>
    <li><a href="javascript:document.getElementById('logoutForm').submit()">Log off</a></li>
</ul>
    }
}
else
{
    <ul class="nav navbar-nav navbar-right">
        <li>@Html.ActionLink("Sign Up", "Singup", "CustomerAccount", routeValues: null, htmlAttributes: new { id = "registerLink" })</li>
        <li>@Html.ActionLink("Log in", "Login", "CustomerAccount", routeValues: null, htmlAttributes: new { id = "loginLink" })</li>
    </ul>
}
